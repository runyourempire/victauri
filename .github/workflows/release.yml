name: Release

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always
  CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

jobs:
  dry-run:
    name: Publish dry-run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev libgtk-3-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Dry-run victauri-core
        run: cargo publish -p victauri-core --dry-run

      - name: Dry-run victauri-macros
        run: cargo publish -p victauri-macros --dry-run

      - name: Dry-run victauri-plugin
        run: cargo publish -p victauri-plugin --dry-run

      - name: Dry-run victauri-test
        run: cargo publish -p victauri-test --dry-run

      - name: Dry-run victauri-cli
        run: cargo publish -p victauri-cli --dry-run

      - name: Dry-run victauri-watchdog
        run: cargo publish -p victauri-watchdog --dry-run

  test:
    name: Test gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev libgtk-3-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install bridge test dependencies
        working-directory: crates/victauri-plugin/tests/bridge_tests
        run: npm install

      - name: Run tests
        run: cargo test --workspace

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: [dry-run, test]
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev libgtk-3-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Publish victauri-core
        run: cargo publish -p victauri-core

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish victauri-macros
        run: cargo publish -p victauri-macros

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish victauri-plugin
        run: cargo publish -p victauri-plugin

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish victauri-test
        run: cargo publish -p victauri-test

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish victauri-cli
        run: cargo publish -p victauri-cli

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish victauri-watchdog
        run: cargo publish -p victauri-watchdog

  github-release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: publish
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
